describe('entitlementsService', function() {

  beforeEach(module('HRMBudget'));

  it('should ...', inject(function(entitlementsService) {

    //expect(entitlementsService.doSomething()).toEqual('something');

  }));

});
