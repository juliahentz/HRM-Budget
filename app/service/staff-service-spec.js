describe('staffService', function() {

  beforeEach(module('HRMBudget'));

  it('should ...', inject(function(staffService) {

    //expect(staffService.doSomething()).toEqual('something');

  }));

});
