describe('salaryService', function() {

  beforeEach(module('HRMBudget'));

  it('should ...', inject(function(salaryService) {

    //expect(salaryService.doSomething()).toEqual('something');

  }));

});
