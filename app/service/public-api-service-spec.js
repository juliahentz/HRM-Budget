describe('publicApiService', function() {

  beforeEach(module('HRMBudget'));

  it('should ...', inject(function(publicApiService) {

    //expect(publicApiService.doSomething()).toEqual('something');

  }));

});
