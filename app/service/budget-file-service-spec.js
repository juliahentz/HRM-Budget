describe('budgetFileService', function() {

  beforeEach(module('HRMBudget'));

  it('should ...', inject(function(budgetFileService) {

    //expect(budgetFileService.doSomething()).toEqual('something');

  }));

});
