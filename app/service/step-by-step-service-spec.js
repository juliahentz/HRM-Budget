describe('stepByStepService', function() {

  beforeEach(module('HRMBudget'));

  it('should ...', inject(function(stepByStepService) {

    //expect(stepByStepService.doSomething()).toEqual('something');

  }));

});
