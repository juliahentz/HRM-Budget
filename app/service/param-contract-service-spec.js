describe('paramContractService', function() {

  beforeEach(module('HRMBudget'));

  it('should ...', inject(function(paramContractService) {

    //expect(paramContractService.doSomething()).toEqual('something');

  }));

});
