describe('dateIntervalService', function() {

  beforeEach(module('HRMBudget'));

  it('should ...', inject(function(dateIntervalService) {

    //expect(dateIntervalService.doSomething()).toEqual('something');

  }));

});
