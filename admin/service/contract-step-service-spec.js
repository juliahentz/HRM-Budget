describe('contractStepService', function() {

  beforeEach(module('admin'));

  it('should ...', inject(function(contractStepService) {

    //expect(contractStepService.doSomething()).toEqual('something');

  }));

});
