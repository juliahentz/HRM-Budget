describe('contractService', function() {

  beforeEach(module('admin'));

  it('should ...', inject(function(contractService) {

    //expect(contractService.doSomething()).toEqual('something');

  }));

});
